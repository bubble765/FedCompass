# FedCompass Docker 部署说明

## 前置条件

- Linux 服务器已安装 Docker Engine。
- 已安装 Docker Compose v2。
- 服务器上保留 real_data 目录。
- 服务器防火墙允许访问 8080 端口。

## 构建和启动

在项目根目录执行：

    docker compose build
    docker compose up -d

后端镜像默认按 CPU 场景安装 PyTorch：x86_64 使用 PyTorch CPU wheel，ARM64 使用不带 CUDA 依赖的 PyTorch wheel。若服务器访问官方 Debian/PyPI 源较慢，可以只在构建时临时指定镜像源：

    docker compose build \
      --build-arg DEBIAN_MIRROR=mirrors.aliyun.com \
      --build-arg PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple

这两个参数不会写入运行时环境，也不会影响部署包中的默认配置。

查看服务状态：

    docker compose ps
    docker compose logs -f backend
    docker compose logs -f frontend

访问：

    http://服务器IP:8080

健康检查：

    curl http://服务器IP:8080/healthz

预期返回：

    {"status":"ok"}

## 数据持久化

SQLite 数据库存放在 Docker volume fedcompass_db 中。普通停止和启动不会丢失实验记录：

    docker compose down
    docker compose up -d

不要在未确认的情况下执行以下命令，因为它会删除数据库卷：

    docker compose down -v

real_data 以只读方式从宿主机挂载：

    ./real_data:/data/real_data:ro

如果部署包不包含 real_data，系统仍可启动并使用 mock/演示能力，但真实数据实验可能不可用。

## 配置助手

当前 Docker Compose 默认同时启动一个本地 Qwen3-0.6B 模型服务。运行保障、安全监控和结果分析智能体直接通过容器内网络调用该服务；实验场景智能体仍保留页面请求级的 API/本地模型切换，如果本地模型暂时不可用，配置助手会回退到确定性的本地规则解析。

配置页面中的“模型来源”可以按请求选择“本地模型”或“API 模型”。两种方式都由后端代理 OpenAI-compatible 的 `/chat/completions` 请求；前端填写的 API Key 只随当前请求传递，不写入数据库、浏览器存储或响应。

本地模型选项固定使用 Qwen-0.6B，页面不展示本地模型服务地址。Compose 会启动 `model` 服务，并将项目中的以下权重复制进模型镜像：

    models/qwen3-0.6b/Qwen3-0.6B-Q8_0.gguf

该文件是 Qwen3-0.6B 的 Q8_0 GGUF 量化版本，约 610 MiB。模型容器在 Docker 网络内监听 8001 端口，后端通过 `http://model:8001/v1` 访问，宿主机不需要单独启动模型服务，也不需要把 8001 暴露给外部网络。当前模型容器按 CPU 模式启动（`--gpu-layers 0`），适合通用 Linux 服务器；如部署环境有专用 GPU，应另行制作匹配 CUDA/Metal 的模型镜像。

如果需要让后端在没有页面参数时使用服务端默认模型，Compose 已配置：

    LLM_PROVIDER=local
    LLM_LOCAL_BASE_URL=http://model:8001/v1
    LLM_LOCAL_MODEL=Qwen/Qwen3-0.6B

后台三个智能体使用独立的本地模型配置，不会被实验场景智能体的请求级模型选择覆盖：

    AGENT_LLM_PROVIDER=local
    AGENT_LLM_LOCAL_BASE_URL=http://model:8001/v1
    AGENT_LLM_LOCAL_MODEL=Qwen/Qwen3-0.6B
    AGENT_LLM_TIMEOUT_SECONDS=180

运行中的运行保障和安全监控默认每 3 轮检查一次，最后一轮无论是否整除 3 都会检查：

    RUNTIME_AGENT_CHECK_INTERVAL_ROUNDS=3

构建时模型权重会被纳入 Docker 构建上下文，因此首次执行 `docker compose build` 会比不含模型时占用更多磁盘和传输时间。`.dockerignore` 特意没有排除 `models/` 或 `*.gguf`；请不要删除模型文件，否则 `model` 镜像无法构建。

如果需要启用服务端默认的外部 API 模型，不要把密钥写入镜像，可以在运行时注入：

    LLM_API_KEY=运行时密钥
    LLM_BASE_URL=服务地址
    LLM_MODEL=模型名称
    LLM_PROVIDER=api

## 迁移已有数据库

当前开发数据库位于：

    0623v1李-后端/fedcompass.db

迁移前先停止服务并备份原文件。然后将数据库复制到 Docker volume，或者修改 Compose 临时挂载一个宿主机目录：

    ./docker-data:/data

迁移后启动服务，并检查实验、结果和事件数据是否存在。

## 停止和更新

更新代码后重新构建并启动：

    docker compose build
    docker compose up -d

查看最近日志：

    docker compose logs --tail=200 backend
    docker compose logs --tail=200 frontend

如果某些 Docker Desktop 版本的 Compose Bake 在构建时报告 BuildKit 会话错误，可以绕过 Compose 构建阶段，直接构建同名镜像后启动：

    docker build \
      --build-arg DEBIAN_MIRROR=mirrors.aliyun.com \
      --build-arg PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple \
      -t fedcompass-backend -f docker/backend/Dockerfile .
    docker build -t fedcompass-frontend -f docker/frontend/Dockerfile .
    docker compose up -d --no-build

## 当前部署边界

- 当前方案使用单个后端容器和 SQLite。
- 实验运行状态和 SSE 事件依赖单进程内存。
- 不建议直接扩展多个 backend 副本。
- 后端重启时，遗留的 running 实验会按应用当前逻辑标记为 stopped。
- 后续需要高可用时，应先迁移 PostgreSQL 和共享任务队列，再扩展多副本。
