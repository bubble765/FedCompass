# 实验场景智能体：模型接入与本地模型选型

## 当前实现

实验场景智能体支持两种请求级模型来源：

1. **本地模型**：前端填写本地 OpenAI-compatible 服务地址和模型名称，适合连接 vLLM、llama.cpp server、Ollama 或其他兼容 `/chat/completions` 的服务。
2. **API 模型**：前端填写自己的 API Base URL、API Key 和模型名称，后端代理请求模型，不让浏览器直接跨域调用外部模型。

请求级配置只保存在当前页面内存中。API Key 不写入 `localStorage`、数据库、Docker 镜像或响应；后端异常信息也不会输出请求密钥。模型返回结果仍会经过实验配置字段白名单、模块选项约束和缺失字段检查。

如果模型服务不可用，配置助手会回退到已有的本地规则解析。因此小模型只负责提高自然语言理解和场景补全能力，不能绕过后端配置约束。

## 候选模型

下面的模型都可以从官方模型卡进入。当前运行环境没有可用浏览器实例，且外网 DNS/HTTPS 访问被环境限制，无法在本轮直接打开这些页面；链接保留为部署前的官方核验入口，许可证、上下文长度和量化文件应以模型卡当前版本为准。

| 档位 | 候选 | 适合本项目的原因 | 主要注意点 |
| --- | --- | --- | --- |
| 约 0.5B | [Qwen3-0.6B](https://huggingface.co/Qwen/Qwen3-0.6B) | 中文和多语言场景更匹配；体量小；适合先验证本地服务、字段抽取和简单场景补全 | 复杂场景推理和严格 JSON 稳定性有限，需要保留规则兜底和后端校验 |
| 约 0.5B，成熟备选 | [Qwen2.5-0.5B-Instruct](https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct) | 指令模型路径成熟，适合低资源部署和结构化任务基线 | 能力上限低于更大模型；建议作为兼容性备选，不作为主要默认模型 |
| 约 1.5B | [Qwen2.5-1.5B-Instruct](https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct) | 体量仍小，中文指令遵循和 JSON 输出通常比 0.5B 档更稳；适合作为稳定基线 | 需要更大的内存和更长的推理时间；仍应限制到配置字段白名单 |
| 约 1.7B | [Qwen3-1.7B](https://huggingface.co/Qwen/Qwen3-1.7B) | 新一代小模型，作为本地默认模型的能力/资源折中更好；适合高层实验场景补全 | 需要确认所选推理服务对 Qwen3 chat template 和 thinking 开关的支持；配置生成建议关闭 thinking |

严格来说，Qwen 系列常见的下一档是 1.5B 或 1.7B，并不是精确的 2.5B。若项目必须使用约 2.5B，应另行核对具体模型的权重规模和许可证；当前优先选 1.7B，比为了凑参数量引入中文能力或部署生态不明确的模型更稳妥。

## 建议落地顺序

### 第一阶段：Qwen3-0.6B

建议先部署 `Qwen/Qwen3-0.6B` 的 4-bit 量化版本，验证以下链路：

- 自然语言场景识别；
- 当前实验配置字段的完整补全；
- 只输出 JSON 的稳定性；
- API 断开时的本地规则兜底；
- CPU 或 Apple Silicon 上的响应时间。

本项目的场景建议中已经有确定性模板，因此 0.6B 不需要独立承担所有推理。更合适的职责是把用户表达映射到候选字段，模板、枚举约束和缺失字段检查由后端负责。

### 第二阶段：Qwen3-1.7B

如果 0.6B 在复杂描述、中文约束或 JSON 输出上不稳定，优先升级到 `Qwen/Qwen3-1.7B`。如果部署环境更看重成熟的指令模型行为，则选择 `Qwen2.5-1.5B-Instruct` 作为对照基线。

建议用以下指标做小规模验收，而不是只看通用问答效果：

- 配置字段识别准确率；
- 场景模式下完整配置字段覆盖率；
- 缺失字段误报/漏报率；
- 非法枚举值被后端拦截的比例；
- 合法 JSON 一次解析成功率；
- 单次请求延迟和峰值内存。

## 本地服务部署边界

模型权重不打进 FedCompass 的业务镜像。推荐将模型服务独立运行，再让页面填写其地址：

```text
本地服务地址：http://127.0.0.1:8001/v1
模型名称：Qwen/Qwen3-0.6B
```

例如，可以使用支持 OpenAI-compatible 接口的服务框架：

- [vLLM OpenAI-compatible server](https://docs.vllm.ai/en/latest/serving/openai_compatible_server.html)
- [llama.cpp server](https://github.com/ggml-org/llama.cpp/tree/master/tools/server)
- [Qwen 官方 GitHub 组织](https://github.com/QwenLM)

Docker 部署时，后端容器内的 `127.0.0.1` 指向后端容器自身。如果模型服务运行在宿主机，页面或服务端默认配置应使用 `host.docker.internal`；本项目 Compose 已加入 `host-gateway` 映射，Linux 服务器上也可以据此连接宿主机服务。

## 安全与生产注意事项

- API Key 目前按请求传递，不持久化；生产环境仍应使用 HTTPS。
- 后端允许用户提供 OpenAI-compatible Base URL，生产部署应增加出口域名白名单或 SSRF 防护。
- 模型服务只应返回配置草案，不能让模型直接创建或启动实验。
- 不要把模型权重、API Key 或量化缓存提交到项目仓库或 Docker 镜像。
- 小模型输出必须继续经过 `CONFIG_FIELDS`、模块枚举和 `ExperimentConfig` 校验；不能把模型文本直接合并进实验配置。
