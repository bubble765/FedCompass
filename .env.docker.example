# Docker 生产环境默认不启用外部 LLM，配置助手会使用本地规则解析。
# 如后续需要启用 OpenAI-compatible 服务，再将以下值注入 backend 容器。
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini
LLM_TIMEOUT_SECONDS=20

# 数据库和 real_data 路径由 docker-compose.yml 固定为容器内路径。
# 如需更换前端端口，请同时修改 docker-compose.yml 和 CORS_ORIGINS。
