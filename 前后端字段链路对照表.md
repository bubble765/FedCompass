# 前后端字段链路对照表

本文档按当前项目真实代码链路梳理“前端字段 -> 后端 schema -> runner 消费 -> 监控指标 -> 结果字段”。

| 前端字段 | 后端 schema | runner 消费 | 监控页指标/事件 | 结果页字段 |
| --- | --- | --- | --- | --- |
| `module_id` | `ExperimentConfig.module_id` | 通过 `resolve_experiment_config()` 归一化为模块与任务路由信息 | 不直接展示，体现在任务行为与模板联动 | 间接影响算法/防御/任务类型 |
| `task_type` | `ExperimentConfig.task_type` | 决定任务分支：`vision_classification` / `defense_demo` / `reid` / `ulip3d` | 决定 `test_accuracy` / `map` / `rank1` / `detection_accuracy` 哪些会持续更新 | `task_type` |
| `dataset` | `ExperimentConfig.dataset` | 决定数据集元信息、基准精度、ReID/3D 指标曲线 | 影响每轮 `round_complete` 里的指标走势 | `dataset` |
| `split` | `ExperimentConfig.split` | 影响 vision 任务的异质性与漂移惩罚 | 影响 `train_loss`、`test_accuracy`、`gradient_drift` | 间接反映在精度与损失 |
| `network` | `ExperimentConfig.network` | 决定 mock 网络实例与通信开销估计 | 影响 `communication_cost_mb`，并写入 `round_complete.network` | 间接反映在轮次过程和最终结果 |
| `algorithm` | `ExperimentConfig.algorithm` | 决定本地训练与聚合插件 | 决定 `round_complete.aggregator` 与各指标趋势 | `algorithm` |
| `optimizer` | `ExperimentConfig.optimizer` | 当前仅入库保留，后续可继续细化进训练策略 | 暂无独立展示 | 暂无独立结果列 |
| `defense` | `ExperimentConfig.defense` | 决定是否走防御过滤、可信客户端筛选 | 影响 `defense_detection` 事件与 `detection_accuracy` | `defense`、`detection_accuracy` |
| `attack` | `ExperimentConfig.attack` | 决定是否注入攻击插件 | 影响恶意客户端、检测结果与精度曲线 | `attack` |
| `num_clients` | `ExperimentConfig.num_clients` | 决定客户端状态数与参与热力图规模 | 影响 `client_participation` 事件和热力图 | 间接影响结果 |
| `participation_rate` | `ExperimentConfig.participation_rate` | 决定每轮采样客户端数 | `client_participation_rate`、热力图、漂移 | `participation_rate` |
| `rounds` | `ExperimentConfig.rounds` | 决定总轮数 | `current_round` / `rounds` / 轮次曲线 | `communication_rounds` |
| `local_epochs` | `ExperimentConfig.local_epochs` | 影响各算法本地训练强度 | 影响 `train_loss` / `test_accuracy` 收敛速度 | 间接反映在结果 |
| `batch_size` | `ExperimentConfig.batch_size` | 当前用于估算通信开销 | `communication_cost_mb` | 间接反映在结果 |
| `learning_rate` | `ExperimentConfig.learning_rate` | 传入各算法本地训练函数 | 影响收敛曲线 | 间接反映在结果 |
| `seed` | `ExperimentConfig.seed` | 控制客户端采样和每轮随机过程 | 影响全套监控轨迹 | `seed` |
| `silence_rate` | `ExperimentConfig.silence_rate` | 为低参与/沉默客户端场景预留 | 未来可直接驱动沉默客户端 | 暂无 |
| `trust_threshold` | `ExperimentConfig.trust_threshold` | 传入 VERT 等防御筛选阈值 | 影响 `trusted_clients` / `filtered_clients` | 间接影响 `detection_accuracy` |
| `distill_weight` | `ExperimentConfig.distill_weight` | 传入 `FedCADS` 本地训练 | 影响蒸馏相关收敛表现 | 间接反映在结果 |
| `global_distill_weight` | `ExperimentConfig.global_distill_weight` | 已入 schema，当前作为扩展位 | 暂未单独展示 | 暂无 |
| `temperature` | `ExperimentConfig.temperature` | 已入 schema，当前作为扩展位 | 暂未单独展示 | 暂无 |
| `lambda_anchor` | `ExperimentConfig.lambda_anchor` | 传入 `CO-EVO` 本地训练 | 影响 ReID 指标走势 | 间接体现在 `map` / `rank1` |
| `lambda_style` | `ExperimentConfig.lambda_style` | 传入 `CO-EVO` 本地训练 | 影响 ReID 域泛化走势 | 间接体现在 `map` / `rank1` |
| `adapter_type` | `ExperimentConfig.adapter_type` | 传入 `FedULIP` 本地训练 | 影响 3D 多模态对齐表现 | 间接体现在 `test_accuracy` |
| `protocol` | `ExperimentConfig.protocol` | 传入 `FedULIP` 协议分支 | 影响 3D 任务难度与曲线 | 间接反映在结果 |
| `runner_type` | `ExperimentConfig.runner_type` | 默认走 `fedcompass` runner | 不直接展示 | 不直接展示 |

## 监控页后端输出

- `GET /api/experiments/{id}`：实验基础信息，包含 `current_round`、`rounds`、`num_clients`、`status`
- `GET /api/experiments/{id}/metrics`：
  - `history[].train_loss`
  - `history[].test_accuracy`
  - `history[].map`
  - `history[].rank1`
  - `history[].detection_accuracy`
  - `participation[]`
  - `defense[]`
  - `drift`
- `GET /api/experiments/{id}/events/stream`：
  - `round_complete`
  - `client_participation`
  - `defense_detection`
  - `completed`
  - `stopped`

## 结果页后端输出

- `GET /api/results`
- `GET /api/results/recent`
- `POST /api/results/compare`

统一结果字段：

- `experiment_id`
- `experiment_name`
- `algorithm`
- `dataset`
- `task_type`
- `best_accuracy`
- `final_accuracy`
- `best_loss`
- `communication_rounds`
- `status`
- `attack`
- `defense`
- `participation_rate`
- `seed`
- `map`
- `rank1`
- `detection_accuracy`
